## Cursor MCP Usage Rules

- Do not invoke the `read_wiki_contents` endpoint from the DeepWiki MCP server. It crashes the application. Use other DeepWiki endpoints instead (e.g., `read_wiki_structure`, `ask_question`).

