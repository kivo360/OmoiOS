# =============================================================================
# OmoiOS Frontend Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values

# =============================================================================
# API Configuration
# =============================================================================

# Backend API URL (defaults to localhost:18000 for local development)
NEXT_PUBLIC_API_URL=http://localhost:18000

# Public site URL (for SEO meta tags, OG images, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# Sentry Error Tracking
# =============================================================================
# Get these from https://sentry.io -> Settings -> Projects -> Client Keys (DSN)

# Public DSN for client-side error tracking (safe to expose)
NEXT_PUBLIC_SENTRY_DSN=

# Environment identifier for Sentry (development, staging, production)
NEXT_PUBLIC_SENTRY_ENVIRONMENT=development

# Enable Sentry debug mode in development (optional)
# NEXT_PUBLIC_SENTRY_DEBUG=true

# Auth token for source map uploads (keep secret!)
# Generate at https://sentry.io -> Settings -> Auth Tokens
SENTRY_AUTH_TOKEN=

# Organization and project slugs for source map uploads
SENTRY_ORG=
SENTRY_PROJECT=

# =============================================================================
# Feature Flags (optional)
# =============================================================================

# Enable debug features
# NEXT_PUBLIC_DEBUG=false

# =============================================================================
# Analytics (optional)
# =============================================================================

# PostHog analytics key (if using PostHog)
# NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com
